<nav style="display: flex; justify-content: space-between; align-items: center; padding: 1rem; background-color: #f4f4f4; border-bottom: 1px solid #ccc;">
  <h1 style="margin: 0; font-size: 1.8rem;">Movies List</h1>

  <%= link_to "Back to Lists", lists_path, class: "btn-add-list" %>
</nav>

<%= simple_form_for @list, html: { class: "simple-form" } do |f| %>
  <%= f.input :name, label: "List Name" %>
  <%= f.button :submit, "Create List", class: "btn-add-list" %>
<% end %>
